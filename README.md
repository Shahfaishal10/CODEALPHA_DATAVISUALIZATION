# Task 3 — Data Visualization
1. Keep `Delinquency_prediction_dataset.xlsx` beside the notebook.
2. Run `python -m pip install -r requirements.txt`.
3. Open `Task_3_Data_Visualization.ipynb`.
4. Select the Python kernel where packages were installed.
5. Click **Run All**.
6. Open `Geldium_Task3_Dashboard.html` in Chrome/Edge.
